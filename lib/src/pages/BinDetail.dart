import 'package:flutter/material.dart';

class BinDetail extends StatelessWidget {
  BinDetail({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: new AppBar(
        title: new Text('Empty my bin'),
      ),
    );
  }
}